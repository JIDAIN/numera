name: CI

on:
  push:
  pull_request:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Quality gates
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Format C1 closure files
        shell: bash
        run: |
          files=(
            src/app/page.tsx
            src/app/globals.css
            src/app/c1-session.integration.test.tsx
            src/components/AHomeTraining.tsx
            src/components/C1HomeTraining.tsx
            src/components/C1HomeTraining.test.tsx
            src/components/C1ScalingTraining.tsx
            src/components/C1ScalingTraining.test.tsx
            src/components/C1SessionInsights.tsx
            src/components/C1SessionInsights.test.tsx
            src/components/SessionDetails.tsx
            src/components/SessionDetails.test.tsx
            src/lib/c1-training.ts
            src/lib/c1-training.test.ts
            src/lib/c-project-registry.ts
            src/lib/c-project-registry.test.ts
            src/lib/runtime-foundation.test.ts
            src/lib/session.test.ts
            src/lib/storage.ts
            src/lib/training-renderer.ts
            src/lib/training.ts
            src/lib/training.test.ts
            src/lib/types.ts
            docs/product/README.md
            docs/product/ui.md
            docs/domain/README.md
            docs/architecture/README.md
            docs/architecture/training-runtime.md
            docs/engineering/current-state.md
            docs/engineering/first-layer-plan.md
          )

          npx --no-install prettier --write "${files[@]}"
          if ! git diff --quiet -- "${files[@]}"; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add -- "${files[@]}"
            git commit -m "style: format C1 closure files"
            git push origin HEAD:master
          fi

      - name: Check types
        run: npm run typecheck

      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Build application
        run: npm run build
