# Claude 入口

本文件只做薄路由，不维护第二份项目规则。

开始任务前依次读取：

1. `AGENTS.md`
2. `.agents/skills/numera-maintainer/SKILL.md`
3. `PROJECT_STATUS.md`
4. 当前任务对应的 `docs/adr/`、`docs/features/` 或 `docs/reference/` 文档和真实代码 / 测试

文档职责与历史归档规则见 `docs/README.md`。如有冲突，以 `AGENTS.md`、当前代码和测试事实为准。
