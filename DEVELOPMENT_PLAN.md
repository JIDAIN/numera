# 数感 Numera：当前开发计划

> 更新时间：2026-09-14  
> 当前阶段：第一层 A 已投入使用，下一阶段进入 C 的产品设计与实现。

## 一、已完成基线

### 1. 项目身份与部署治理

- 正式品牌统一为：数感 / Numera / 算算；
- GitHub 仓库：`JIDAIN/numera`；
- Vercel Project：`numera`；
- Production：`https://fish-cat-speed-math.vercel.app`；
- Git 自动部署关闭，Production 必须获得明确授权后手动执行。

### 2. 第一层 A 架构收口

正式 A 只保留 8 个 canonical ability：

- `A-ADD-01` 2～3 位加法；
- `A-SUB-01` 2～3 位减法；
- `A-COM-01` 近邻小差值；
- `A-MUL-01` 正向乘法口诀；
- `A-MUL-02` 逆向乘法口诀；
- `A-MUL-03` 两位数 × 一位数；
- `A-FRA-01` 高频分数 ↔ 百分数固定映射；
- `A-PCT-01` 基础百分比取值。

旧 160 叶子架构已经退出正式 runtime，不再作为产品能力体系或兼容约束。

### 3. A 训练闭环

已经具备：

- 专项生成；
- L1 / L2 / L3 难度；
- 作答、判题与计时；
- 结构标签与生成参数；
- TrainingSession / QuestionRecord / completed；
- Mastery；
- 历史趋势；
- 冻结题组；
- 数据导出；
- 异步 PK 基础设施。

2026-09-12 最终清理回归：typecheck、lint、237 / 237 tests、Production Build 全部通过。

2026-09-14 Fish / Cat 使用真实账号完成 A-PK 人工端到端验收，发起、完成和双方结果查看均正常。

### 4. CI 历史格式基线处理

仓库仍有一批历史文件不符合当前 Prettier，但不再允许这批旧债务永久阻塞所有 CI。

当前策略：

- CI 只对本次 push / PR 修改的可格式化文件执行 Prettier；
- typecheck、lint、test、build 始终对整个项目执行；
- 以后任何被修改的旧文件，必须在本次修改中同时满足当前格式；
- 全仓 `npm run format:check` 继续保留，用于单独审计历史格式债务；
- 如要一次性清零格式债务，单独建立“纯格式化”批次，不与功能开发混合。

## 二、当前架构边界

### A：底层自动化

已锁定并投入使用。除非真实训练数据证明现有能力边界有明显问题，否则不再随意拆出新的微能力。

A 层准入仍坚持：高频调用、跨场景复用、可独立训练、有明显迁移收益。

### B：数字变形参考

当前不建立独立 Mastery，也不建设独立“B 能力树”。

B 只作为：

- 方法训练的关键步骤；
- 解析语言；
- 方法标签；
- 诊断标签。

后续由 C 的真实方法需求自然整理 B，而不是先凭空枚举大量变形动作。

### C：完整纯计算任务

当前只保留：

1. C1 乘法综合；
2. C2 除法综合；
3. C3 分数比较。

原则：**方法训练把方法显式化；综合训练把方法选择交还给用户。**

方法训练只要求填写真正值得训练的关键步骤；综合训练只提供原始题目和最终答案。两种训练长期并列，都需要讲解，不设置强制“学习 → 引导 → 独立 → 综合”生命周期。

## 三、下一阶段开发顺序

### P1：C1 乘法综合

目标：完成乘法完整任务的产品设计，而不是恢复旧“两位数 × 两位数题型”就算完成。

需要先审清：

- 资料分析里真正高频的乘法数字结构；
- 用户实际使用的拆分、凑整、邻近整十/整百、特殊因子等方法；
- 哪些属于 A 的直接调用，哪些只是 B 参考步骤；
- 方法训练应该显式填写哪些关键中间量；
- 综合训练如何只给原始算式、让用户自己选方法；
- 每种方法什么情况下值得用，什么情况下不值得用；
- 讲解、结构标签、计时和诊断如何落库。

在产品规则没有锁定前，不进入大规模 UI 或生成器开发。

### P2：C2 除法综合

在 C1 方法训练 / 综合训练框架稳定后，再设计除法综合。

重点包括：

- 直除；
- 邻近倍数反应；
- 放缩估算；
- 基准数选择；
- 一阶修正；
- 商首位 / 有效位 / 精度要求；
- 什么结构适合某种方法，什么结构应该直接换方法。

以前的放缩法和相关实验代码只能作为研究素材，不能直接恢复成旧叶子能力体系。

### P3：C3 分数比较

重新从完整任务出发设计，而不是简单沿用旧题型结构。

需要审查：

- 交叉乘；
- 同向变化；
- 靠近 1/2、1、常用锚点；
- 分子分母差值；
- 放缩与估算；
- 不同方法的适用条件与停止计算时机。

### P4：由 C 整理 B 参考库

完成 C1～C3 后，把真正反复出现的数字变形抽出来形成稳定 B 参考词汇和标签集。

B 仍不建立永久 Mastery，除非未来有明确证据证明某个动作满足 A 的准入标准。

### P5：进入第二层资料分析专用计算方法

按真实资料分析题型系统拆解：

`资料分析题型 → 目标表达式 → 可选计算路径 → 第一层能力调用`

优先研究：

- 一般增长率；
- 基期量与增长量；
- 间隔增长率；
- 比值增长率 / 乘积增长率；
- 现期 / 基期比重；
- 平均数、倍数；
- 盐水 / 十字交叉；
- 年均增长率；
- 化除为乘、415、假设分配等资料分析专用路径。

第二层不要重新制造“分式加减乘除微能力树”。

### P6：第三层实战判断与决策

最后进入真实材料题：

- 时间、对象、指标、范围、单位识别；
- 目标量和公式判断；
- 方法选择；
- 选项距离与精度决策；
- 停止计算时机；
- 错因与慢因复盘。

## 四、工程质量门

每个功能批次至少需要：

1. 修改文件通过 Prettier；
2. `npm run typecheck`；
3. `npm run lint`；
4. `npm run test`；
5. `npm run build`；
6. 关键交互补自动化测试；
7. 必要时做真实账号 / 真机人工验收；
8. 同步 `README.md`、`PROJECT_STATUS.md`、本文件和数感 Obsidian 正式文档。

CI 对修改文件做格式检查，对整个项目执行类型、lint、测试和构建。

## 五、当前明确不做

- 不恢复旧 160 叶子体系；
- 不把 B 变成独立 Mastery 树；
- 不把方法步骤保存为微能力 ID；
- 不强行把经典历史重写为新 A；
- 不在 C 规则尚未锁定时先大规模编码；
- 不顺手进行全仓大面积 Prettier 改写；
- 不未经明确授权部署 Production；
- 暂不扩展实时 PK、排行榜、每日任务、active 跨设备同步或新的 PWA 离线能力。

## 六、事实源

当前开发判断以以下内容为准：

- `README.md`；
- `PROJECT_STATUS.md`；
- 本文件；
- `JIDAIN/lys-obsidian-note/13_Projects/数感/`；
- 当前 `master` 代码和测试。

更早的旧阶段计划、旧题型审计和旧 160 叶子实验文档仅保留追溯价值；与当前事实源冲突时，不代表现行开发计划。
