# 数感 Numera：当前开发计划

> 更新时间：2026-09-15  
> 当前起点：A V1 已投入使用，当前进入 C1 乘法综合。

本文件只维护**从当前状态向后的开发顺序与验收要求**，不重复记录已经完成的 PR、CI、部署和安全修复历史。

## P1：C1 乘法综合

先锁定产品规则，再编码。

需要明确：

- 资料分析中真正高频的乘法数字结构；
- 哪些情况适合直接算，哪些适合拆分、凑整、邻近整十/整百或特殊因子；
- 哪些动作只是 A 的直接调用，哪些应作为 B 方法步骤或诊断标签；
- 方法训练需要用户显式填写哪些关键中间量；
- 综合训练怎样只给原始算式与最终答案，把方法选择交还给用户；
- 方法适用条件、讲解、计时、标签和数据记录怎样落库；
- C1 是否需要新的正式任务 ID，以及如何与 A Mastery、经典历史和导出兼容。

规则锁定后再进入生成器、会话、UI 与测试实现。

## P2：C2 除法综合

重点审查：

- 普通直除；
- 邻近倍数与试商；
- 除法拆分；
- 放缩估算；
- 基准数选择；
- 一阶修正；
- 商首位 / 有效位 / 精度要求；
- 不同数字结构下的方法切换；
- 方法训练与综合训练的边界。

已有旧实验代码或旧文档只能作为参考，不能直接恢复为现行产品规则。

## P3：C3 分数比较

重点审查：

- 交叉乘；
- 同向变化；
- 靠近 `1/2`、`1` 或常用锚点；
- 分子分母差值；
- 放缩与估算；
- 不同方法的适用条件；
- 选项或目标精度下什么时候应停止继续计算。

## P4：由 C 整理 B 参考库

完成 C1～C3 后，再从真实方法中抽取反复出现的数字变形语言，例如拆分、凑整、选基准、特殊倒数、误差方向与补偿。

B 仍不建立永久独立 Mastery。只有真实训练数据证明某个步骤具备高频、跨场景、可独立训练和明显迁移收益时，才重新讨论是否升级为正式能力。

## P5：第二层资料分析专用计算方法

按真实资料分析题型拆解：

`题型 → 目标表达式 → 可选计算路径 → 第一层能力调用`

优先整理：

- 一般增长率；
- 基期量与增长量；
- 间隔增长率；
- 比值增长率 / 乘积增长率；
- 比重；
- 平均数与倍数；
- 盐水 / 十字交叉；
- 年均增长率；
- 化除为乘、415、假设分配等资料分析专用计算路径。

## P6：第三层资料分析实战判断与决策

最后进入真实材料题中的：

- 时间、对象、指标、范围和单位识别；
- 目标量与公式关系判断；
- 方法选择；
- 选项距离与计算精度决策；
- 停止计算时机；
- 错因与慢因复盘。

## 每个功能批次的质量门

至少要求：

1. 先把产品规则和数据语义写清楚；
2. 修改文件通过 Prettier；
3. `npm run typecheck`；
4. `npm run lint`；
5. `npm run test`；
6. `npm run build`；
7. 关键生成、会话、持久化和交互补自动化测试；
8. 必要时使用真实账号 / 真机做人工验收；
9. 同步 `README.md`、`PROJECT_STATUS.md`、本文件、相关专项文档与 Obsidian 正式知识库，但只更新各文档职责范围内的信息；
10. 未经明确授权不执行 Production 部署。

## 当前不做

- 不恢复旧 160 叶子体系；
- 不把 B 重新做成独立 Mastery 树；
- 不把方法步骤保存成微能力 ID；
- 不强行把经典历史改写为新 A/C 数据；
- 不在 C 规则尚未锁定时先大规模编码；
- 不为了“文档完整”新增重复的 PR / 阶段总结文档；
- 不顺手扩展实时 PK、排行榜、多人、每日任务、active 跨设备同步或新的 PWA 能力。
