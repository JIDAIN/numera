# 数感 Numera：当前开发计划

> 更新时间：2026-09-15  
> 当前阶段：第一层 A V1 进入 PR #4 最终工程收口；通过最终 CI 后转入 C1 产品设计。

## 一、已完成基线

### 1. 项目身份与部署治理

- 正式品牌：数感 / Numera / 算算；
- GitHub：`JIDAIN/numera`；
- Vercel Project：`numera`；
- Production：`https://fish-cat-speed-math.vercel.app`；
- Git 自动部署关闭，Production 必须获得明确授权后手动执行。

### 2. 第一层 A 架构收口

正式 A 只保留 8 个 canonical ability：

- `A-ADD-01` 2～3 位加法；
- `A-SUB-01` 2～3 位减法；
- `A-COM-01` 近邻小差值；
- `A-MUL-01` 正向乘法口诀；
- `A-MUL-02` 逆向乘法口诀；
- `A-MUL-03` 两位数 × 一位数；
- `A-FRA-01` 高频分数 ↔ 百分数固定映射；
- `A-PCT-01` 基础百分比取值。

旧 160 叶子架构已经退出正式 runtime，不再作为产品能力体系或兼容约束。

Canonical A ID 只维护一份实现源。公开能力定义、runtime registry 与测试都引用/校验同一清单；能力元数据单独维护，但不能再形成第二套独立 ID 清单。

### 3. 当前首页与 A 训练闭环

当前首页训练区统一使用 `AHomeTraining`：

- “我的日常”：用户自选 1～8 个 A 能力，并为每个能力设置 L1 / L2 / L3；每次 10 / 20 题；
- “最近专项”：最近完成的本人非 PK A 专项可一键再来一组；
- “全部练习”：直接展示 8 个 A 能力；
- “经典训练”：保留在“更多 → 经典训练”。

早期“六个第一层入口”与旧 `TrainingTypeSelector` / `SkillDrillSelector` 不再代表当前产品结构。

A 训练已经具备：

- 专项生成；
- L1 / L2 / L3；
- 作答、判题与计时；
- 结构标签与生成参数；
- TrainingSession / QuestionRecord / completed；
- Mastery；
- 历史趋势；
- 冻结题组；
- 数据导出；
- 异步 PK 基础设施。

2026-09-14 Fish / Cat 已用真实账号完成 A-PK 人工端到端验收。

### 4. 日常训练规则

新的日常训练只使用用户配置的 `daily_plan`：

- `DailyTrainingPlan.version` 当前必须严格等于 `1`；
- persisted plan 会过滤非法/重复能力；
- entries 按 canonical A 顺序归一化；
- 题目生成后冻结到 TrainingSession；
- 重开从冻结题目恢复能力与难度，并按 canonical A 顺序重建计划，避免 shuffle 改变 10 题余数分配；
- 每道题继续保存正式 A `skillId / difficultyBand / structureTags / generatorParams`。

旧 `mixed:L*` 自动混合链不再有新的用户入口，仅作为旧冻结会话兼容实现保留。

### 5. CI 历史格式基线

当前策略：

- CI 只对本次 push / PR 修改的可格式化文件执行 Prettier；
- typecheck、lint、test、build 始终对整个项目执行；
- 以后任何被修改的旧文件必须同时满足当前格式；
- 全仓 `npm run format:check` 继续用于历史格式债务审计；
- 如需一次性清零格式债务，单独建立纯格式化批次。

## 二、PR #4 最终收口质量门

PR #4 在结束 Draft 前必须满足：

1. 首页、代码与三份仓库事实源一致；
2. canonical A ID 无双事实源漂移风险；
3. 旧用户侧 A/日常 Selector 退出生产路径；
4. `daily_plan` 版本校验与重开分配稳定；
5. 首页 → A 专项 → 冻结 Session 集成测试通过；
6. 首页 → 自定义日常 → 冻结 `daily_plan` Session 集成测试通过；
7. 根目录无意义 `.gitkeep` 删除；
8. Prettier / typecheck / lint / test / build 全绿；
9. 不进行未经授权的 Production 部署。

依赖安装当前报告的 2 moderate + 3 high 漏洞不与 PR #4 功能修改混合，已登记 GitHub Issue #5。Issue #5 需要先取得完整 `npm audit` 路径并判断生产影响，再做最小兼容升级；禁止直接无审查使用 `npm audit fix --force`。

## 三、当前架构边界

### A：底层自动化

已锁定并投入使用。除非真实训练数据证明现有能力边界有明显问题，否则不再随意拆出新的微能力。

### B：数字变形参考

不建立独立 Mastery，也不建设独立“B 能力树”。B 只作为方法步骤、解析语言、方法标签和诊断标签。

### C：完整纯计算任务

当前只保留：

1. C1 乘法综合；
2. C2 除法综合；
3. C3 分数比较。

原则：**方法训练把方法显式化；综合训练把方法选择交还给用户。**

方法训练只填写真正值得训练的关键步骤；综合训练只提供原始题目和最终答案。两种训练长期并列，都需要讲解。

## 四、PR #4 之后的开发顺序

### P1：C1 乘法综合

先锁定产品规则，再编码。重点审查：

- 资料分析中真正高频的乘法数字结构；
- 拆分、凑整、邻近整十/整百、特殊因子等实际方法；
- 哪些属于 A 的直接调用，哪些只是 B 参考步骤；
- 方法训练显式填写哪些关键中间量；
- 综合训练如何只给原始算式并让用户自己选方法；
- 方法适用条件、讲解、标签、计时和诊断如何落库。

### P2：C2 除法综合

重点包括：

- 直除；
- 邻近倍数反应；
- 放缩估算；
- 基准数选择；
- 一阶修正；
- 商首位 / 有效位 / 精度要求；
- 不同数字结构下的方法切换。

### P3：C3 分数比较

重点审查：

- 交叉乘；
- 同向变化；
- 靠近 1/2、1、常用锚点；
- 分子分母差值；
- 放缩与估算；
- 不同方法的适用条件与停止计算时机。

### P4：由 C 整理 B 参考库

完成 C1～C3 后，把真正反复出现的数字变形抽出来形成稳定 B 参考词汇与标签集。B 仍不建立永久 Mastery。

### P5：进入第二层资料分析专用计算方法

按真实题型拆解：

`资料分析题型 → 目标表达式 → 可选计算路径 → 第一层能力调用`

优先研究一般增长率、基期量与增长量、间隔增长率、比值/乘积增长率、比重、平均数、倍数、盐水/十字交叉、年均增长率、化除为乘、415、假设分配等。

### P6：第三层实战判断与决策

最后进入真实材料题：时间/对象/指标/范围/单位识别、目标量和公式判断、方法选择、选项距离与精度决策、停止计算时机、错因与慢因复盘。

## 五、工程质量门

每个功能批次至少需要：

1. 修改文件通过 Prettier；
2. `npm run typecheck`；
3. `npm run lint`；
4. `npm run test`；
5. `npm run build`；
6. 关键交互补自动化测试；
7. 必要时做真实账号 / 真机人工验收；
8. 同步 `README.md`、`PROJECT_STATUS.md`、本文件和数感 Obsidian 正式文档。

## 六、当前明确不做

- 不恢复旧 160 叶子体系；
- 不把 B 变成独立 Mastery 树；
- 不把方法步骤保存为微能力 ID；
- 不强行把经典历史重写为新 A；
- 不在 C 规则尚未锁定时先大规模编码；
- 不顺手进行全仓大面积 Prettier 改写；
- 不在 PR #4 中混入无关的大规模依赖升级；
- 不未经明确授权部署 Production；
- 暂不扩展实时 PK、排行榜、每日任务、active 跨设备同步或新的 PWA 离线能力。

## 七、事实源

当前开发判断以以下内容为准：

- `README.md`；
- `PROJECT_STATUS.md`；
- 本文件；
- `JIDAIN/lys-obsidian-note/13_Projects/数感/`；
- 当前 `master` / 待合并 PR 的实际代码和测试。

更早的旧阶段计划、旧题型审计和旧 160 叶子实验文档仅保留追溯价值；与当前事实源冲突时，不代表现行开发计划。
