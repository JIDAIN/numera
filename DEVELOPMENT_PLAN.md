# 数感 Numera：当前开发计划

> 更新时间：2026-09-21  
> 当前起点：A V1 已投入使用；乘法训练边界已重新收口，先补齐 A 层基础乘法，再实现 C 层方法训练。

本文件只维护**从当前状态向后的开发顺序与验收要求**，不记录已经完成的 PR、CI、部署、迁移和审计流水。

## P1：补齐 A 层乘法

A 层只负责“给定算式后，把基础运算算熟、算快”，不承载方法选择。

当前已锁定新增：

- A-MUL-04 两位数 × 两位数；
- A-MUL-05 百分数 × 百分数。

其中：

- A-MUL-04 只训练基础乘法执行，不把放缩、特殊乘数转换等方法塞回 A；
- A-MUL-05 统一以百分数形式作答，答案保留百分数值小数点后两位；
- 现有 A-MUL-01 正向乘法口诀、A-MUL-02 逆向乘法口诀、A-MUL-03 两位数 × 一位数继续保留；
- 小分互换已有对应基础训练，不在乘法模块重复造轮子；
- 百分比拆分不单独建立训练模块。

A-MUL-04 / 05 已锁定为目标能力，代码实现尚未完成；实现时同步更新 canonical A registry、生成器、测试、日常训练与导出契约。

## P2：C 层乘除方法——特殊基准数乘除转换

C 层承载“识别数字结构 → 改写运算 → 用更便宜的运算得到结果”的方法训练。

首个已锁定的方法族为：

> **特殊基准数乘除转换**

训练目标不是记住某一道题，而是看到特殊基准数后自动把多位数乘除转换成简单个位数乘除与数量级调整。

### L1 简单

基准数：

- 5
- 25
- 125
- 333
- 167
- 143
- 111

用户先选择一个基准数，再选择：

- 乘法；
- 除法；
- 乘除综合。

本级另提供“综合”，用于多基准数混合训练。

### L2 困难

基准结构：

- 667
- 286
- 9
- 11
- aaa 重复数字规律

同样支持：

- 单项乘法；
- 单项除法；
- 单项乘除综合；
- 本级综合。

### L3 复杂

不再引入新的基准数，只训练 L1 / L2 关系的**跨量级迁移**，例如：

- 5 → 0.5 / 50 / 500
- 25 → 2.5 / 250
- 125 → 12.5 / 1250
- 333 → 33.3 / 3.33 / 3330
- 167 → 16.7 / 1.67 / 1670
- 143 → 14.3 / 1.43 / 1430
- 111 → 11.1 / 1.11 / 1110
- 667 → 66.7 / 6.67 / 6670
- 286 → 28.6 / 2.86 / 2860

L3 只需要：

- 乘法综合；
- 除法综合；
- 乘除综合。

### 统一训练规则

当前 V1 统一：

- 正式专项以 20 题为主；
- 单项训练只变基准数对应的另一操作数，不额外区分“精算题 / 估算题”；
- 另一操作数先用随机 3～5 位整数；
- 只提交最终数值；
- 以真实数学结果为基准计算相对误差；
- **相对误差 ≤ 2% 视为通过**；
- 后台继续记录实际相对误差与单题用时。

这里的 2% 是**该专项的训练通过标准**，不是“所有资料分析真题都允许 2% 误差”的结论。真实题所需精度仍由任务和选项决定。

多个基准数的综合训练必须保证覆盖，不采用可能漏掉某个基准数的纯随机抽样。

## P3：C 层乘法方法——乘法放缩

乘法放缩单独作为方法训练，不与 A 的基础乘法混在一起。

已锁定的边界：

- 不限制整数 / 小数 / 百分数；
- 不限制原始位数；
- 复杂资料分析乘法优先放到这里，而不是继续扩 A 的“几位数 × 几位数”分类；
- 核心训练是识别可放缩结构、选择更便宜的目标数、反向补偿并控制误差；
- 不为“精确 / 估算”设置两个训练入口；
- 题目生成应先构造值得放缩的结构，再还原为原始算式，而不是先随机出两个数再碰运气筛选。

下一阶段继续锁定乘法放缩的训练模式、难度、20 题内部结构与交互。

## P4：继续整理除法与分数方法

特殊基准数乘除转换已作为乘除共享方法先行锁定。

其余除法与分数方法继续审查：

- 普通直除与试商；
- 除法放缩；
- 基准数选择与修正；
- 商首位 / 有效位；
- 分数比较中的交叉乘、同向变化、常用锚点与放缩；
- 方法训练与基础运算 A 层之间的边界。

不因为旧实验代码存在就默认恢复为现行产品规则。

## P5：第二层资料分析专用计算方法

按真实资料分析题型拆解：

题型 → 目标表达式 → 可选计算路径 → 第一层能力 / C 方法调用

优先整理：

- 一般增长率；
- 基期量与增长量；
- 间隔增长率；
- 比值增长率 / 乘积增长率；
- 比重；
- 平均数与倍数；
- 盐水 / 十字交叉；
- 年均增长率；
- 化除为乘、415、假设分配等资料分析专用计算路径。

## P6：第三层资料分析实战判断与决策

最后进入真实材料题中的：

- 时间、对象、指标、范围和单位识别；
- 目标量与公式关系判断；
- 方法选择；
- 选项距离与计算精度决策；
- 停止计算时机；
- 错因与慢因复盘。

## 每个功能批次的质量门

至少要求：

1. 先把产品规则和数据语义写清楚；
2. 修改文件通过 Prettier；
3. npm run typecheck；
4. npm run lint；
5. npm run test；
6. npm run build；
7. 关键生成、会话、持久化和交互补自动化测试；
8. 必要时使用真实账号 / 真机做人工验收；
9. 只更新真正受影响的长期文档：当前状态进 PROJECT_STATUS.md，架构决策进 docs/adr/，功能契约进 docs/features/，稳定规则进 docs/reference/；
10. 一次性迁移或审计仅在确有追溯价值时归档到 docs/history/ 或 docs/audits/；
11. 同步 Obsidian 正式知识库中对应职责的内容；
12. 未经明确授权不执行 Production 部署。

完整文档分层规则见 docs/README.md。

## 当前不做

- 不恢复旧 160 叶子体系；
- 不把方法步骤保存成微能力 ID；
- 不为每个特殊基准数建立独立正式 ability ID；
- 不把数量级变体（如 5 / 50 / 500）拆成重复能力；
- 不强行把经典历史改写为新 A/C 数据；
- 不在方法规则尚未锁定时先大规模编码；
- 不把专项训练的 2% 阈值误写成真实资料分析统一精度；
- 不为单次 PR、测试、部署或阶段验收新增长期文档；
- 不顺手扩展实时 PK、排行榜、多人、每日任务、active 跨设备同步或新的 PWA 能力；
- 未经明确授权不执行 Production 部署。
