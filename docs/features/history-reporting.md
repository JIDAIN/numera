# 历史记录与成绩汇总口径

适用版本：2026-08-04 历史记录与成绩汇总升级。

## 数据范围与归属

- 登录后，历史数据源为当前账号明确归属的本地 `completed` 记录，以及 RLS 已允许读取的云端本人和配对对象记录；同一稳定训练 ID 只显示、统计一次。
- 默认查看当前登录账号。登录用户可切换到固定配对对象；对方记录始终只读，不显示同步或重试操作。
- 未同步但已明确归属当前账号的本地 `completed` 记录继续显示并参与本人汇总。
- 未登录时只显示 `ownerAccountId` 缺失的本地 `completed` 记录；已绑定账号的本地记录不会被自动混入未登录视图。
- 未完成的 active 会话不属于历史数据，也不会上传云端。

## 筛选

- 题型筛选仅列出当前所选用户实际拥有的记录类型。
- 选择题型后，子模式继续独立筛选；商首位、商前两位、3%估算、分数转百分数和百分数转分数互不混合。
- 历史列表和汇总始终使用同一组“用户 + 题型 + 子模式”筛选结果。

## 汇总指标

汇总可跨题量，以反映该筛选范围内的总体练习量；它不替代题量隔离的趋势比较。

| 指标         | 计算方式                                                       |
| ------------ | -------------------------------------------------------------- |
| 总训练组数   | 筛选后的 completed 训练数量                                    |
| 总题数       | 所有冻结 `questions.length` 之和                               |
| 总正确率     | 所有正确题数之和 ÷ 总题数，不平均每组百分比                    |
| 平均单题用时 | 所有 `elapsedMs` 之和 ÷ 总题数                                 |
| 最近一次等级 | 按完成训练时间最新的一组的冻结评级；旧记录按其可读评级规则展示 |
| 最佳等级     | `优秀 > 良好 > 合格 > 继续加油` 的最高档                       |
| 等级次数     | 每一评级在筛选结果中出现的训练组数                             |

每条历史卡片显示本地时间、题型和子模式、实际题量、正确题数、总有效用时、平均单题用时、等级及同步状态；点击卡片只打开原始冻结题组与作答复盘。

## 趋势与题量

趋势的比较键保持为“用户 + 题型 + 子模式 + 实际题量”。题量来自冻结题组的实际长度，旧记录没有显式题量时也以此推断。用户必须先选择一个题量，才查看该题量下的速度与正确率趋势；10、20、100 题训练不会默认进入同一条折线。

本页不提供“全部题量综合趋势”，也不改变评级、同步、RLS 或账号隔离规则。
