# 异步 PK（第一版）

## 用户流程

🐟或🐱先完成一组普通训练，在结算页点击“向对方发起PK”。系统以已同步的普通训练为来源，冻结题型、子模式、题量、全部题目及顺序、正确答案、版本、作答、逐题用时、总用时、正确题数和等级。对方只能按这套冻结题组完成一次挑战；两人的完成训练仍分别作为长期个人历史记录保存。

首页的“PK挑战”入口始终可点：红色气泡表示本人尚未完成的挑战数量（未开始与进行中）；蓝色气泡表示对方完成后、本人尚未进入 PK 页接收的结果数量。红蓝同时存在时只显示红色。进入 PK 页面只清除蓝色提醒，红色必须在挑战真正完成后才减少；1～9 显示数字，更多显示 `9+`。

PK 页分为“待我处理”（进行中优先，再按发起时间倒序）、“等待对方”和“近7日已完成”。完成结果按最终完成时间倒序，使用最近 7 个自然日（含当日和前 6 日）；超过范围的结果只从 PK 页隐藏，不删除双方普通训练。

## 胜负与详情

先比较正确题数；相同再比较总有效用时；两者都相同则平局。等级仅展示，不参与胜负。列表页承担双方成绩、等级、胜负和完成时间总览；结果详情只承担完整逐题复盘，严格按冻结题组原顺序显示所有题。每题按稳定题目 ID 关联双方记录，缺失记录明确显示“未作答”；仅当至少一方未答对时显示正确答案。

## 存储、同步与权限

- active PK 只存在开始挑战的当前浏览器，并按真实账号隔离；离页暂停、暂存、恢复与普通训练一致，不跨设备同步。
- PK 完成先写入 IndexedDB 的个人 completed 历史，再同步 completed 训练；完成同步后才幂等提交 PK 结果。失败不会删除本地数据，可沿用个人历史的“重试同步”。
- Supabase 的 `pk_challenges` 只允许固定配对双方读取；创建、提交、结果已读均通过安全定义函数验证 Auth 身份、固定配对关系、发起人/指定对手、冻结题组和唯一训练 ID。非配对账号不可读取或提交。
- 同一来源普通训练有唯一 PK 挑战；同一挑战只能绑定指定对手的一条完成训练。

## 分页

普通历史和近 7 日 PK 结果均使用传统分页：`第 X 页 / 共 Y 页`、上一页和下一页。第一页禁用上一页，末页禁用下一页；筛选改变时回到第 1 页，页数因数据变化减少时自动回到有效页。普通历史默认每页 20 条，PK 每页 10 条。

第一版不支持实时同时答题、多人/好友、排行榜、每日任务、拒绝/撤回、推送、Realtime、跨浏览器 active 恢复、PK 页面直接建题组、永久 PK 历史或无限滚动。

## Supabase 执行与验收

对应迁移位于 [`supabase/migrations/20260804_async_pk.sql`](../../supabase/migrations/20260804_async_pk.sql)。该迁移可重复执行，且不删除现有 Auth、profiles、pairs 或 completed 训练。

真机验收：🐟完成并发起一组训练；🐱在另一设备首页见红泡，开始后退出并重新进入继续；完成后双方普通历史均增加一条个人训练，🐟出现蓝泡；🐟进入 PK 页蓝泡清除，双方在近 7 日看到同一结果和详情；切换账号或未配对账号均不能接管或读取挑战；断网完成后确认个人历史保留，再恢复网络用“重试同步”提交结果。
